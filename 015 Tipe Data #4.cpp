#include <iostream>
using namespace std;

int main() {
    cout<<"Kata-Kata \"Keywords\" dalam C++ yang tidak bisa digunakan (variable, fungsi, dll) : ";
    cout<<"asm else new this auto enum operator throw break export protected try case extern public typedef catch false register typeid char float reinterpret_cast typename class for return union const friend short unsigned continue if sizeof virtual default inline static void delete int static_cast volatile do long struct wchar_t double mutable switch while dynamic_cast namespace template";
}
